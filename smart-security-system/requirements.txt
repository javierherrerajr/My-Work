# Smart Security System - Main Computer Requirements
# Install with: pip3 install -r requirements.txt

# Core packages
opencv-python>=4.8.0
Pillow>=9.5.0
requests>=2.31.0
numpy>=1.21.0

# GUI framework
# tkinter is included with Python, no installation needed

# Face recognition (required for webcam mode)
dlib>=19.24.0
face_recognition>=1.3.0

# Note: If dlib installation fails on macOS, first install:
# brew install cmake
# Then retry: pip3 install -r requirements.txt